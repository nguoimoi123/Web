// /** @type {import('next').NextConfig} */
// const nextConfig = {
//     experimental: {
//     },
//   };
  
//   export default nextConfig;
 /** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['localhost'], // Thêm miền tại đây
  },
};

export default nextConfig;
